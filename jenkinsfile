pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                sh 'echo "Hello World"'
                sh " whoami"
                sh " docker --version"
                sh " docker build -t my-site ./web"
            }
        }
    }
}
